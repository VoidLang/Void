#include "StackElement.hpp"

/*
namespace Void
{
    template <typename T>

    /**
     * Push a value to the top of the stack.
     * /
    void StackElement<T>::push(T value) 
    {
        data.push_back(value);
    }

    template <typename T>

    /**
     * Append a value to the beginning of the stack.
     * /
    void StackElement<T>::append(T value)
    {
        data.insert(data.begin(), value);
    }

    template <typename T>

    /**
     * Pull the first element of the stack.
     * /
    T StackElement<T>::pull()
    {
        T value = data[0];
        data.erase(data.begin());
        return value;
    }

    template <typename T>

    /**
     * Get the first element of the stack.
     * /
    T StackElement<T>::get()
    {
        return data[0];
    }

    template <typename T>

    /**
     * Get the size of the stack.
     * /
    SIZE_T StackElement<T>::size()
    {
        return data.size();
    }
}
*/