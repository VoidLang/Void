#include "StorageElement.hpp"

/*
namespace Void
{
    template <typename T>

    /**
     * Set the value of the given sub-storage unit.
     * /
    void StorageElement<T>::set(int index, T value)
    {
        data[index] = value;
    }

    template <typename T>

    /**
     * Get the value of the given sub-storage unit.
     * /
    T StorageElement<T>::get(int index)
    {
        return data[index];
    }

    template <typename T>

    /**
     * Remove the value of the given sub-storage unit.
     * /
    void StorageElement<T>::pop(int index)
    {
        data.erase(index);
    }
}
*/